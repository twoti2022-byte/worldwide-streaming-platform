# Future integration boundaries

ChatGPT and video services are server-only stubs. ChatGPT must call platform APIs through a permission layer rather than querying the database directly. Cloudflare Stream integration is reserved for future upload, processing, status, playback, and deletion modules.
