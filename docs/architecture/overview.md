# Architecture overview

The repository separates application UI, reusable components, API helpers, Supabase infrastructure, security, logging, validation, and service boundaries. OpenAI and Cloudflare credentials are reserved for server-side code only.

Future modules may add approved API namespaces without bypassing authorization or row-level security.
