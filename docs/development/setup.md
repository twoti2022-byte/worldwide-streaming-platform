# Development setup

Required checks are `npm run build`, `npm run lint`, `npm run typecheck`, and `npm run test`. Run Supabase migrations from `supabase/migrations` against a configured Supabase project. Keep service-role, OpenAI, and Cloudflare credentials out of browser code.
